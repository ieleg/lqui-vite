import { Meta, Canvas, Story, ArgsTable } from "@storybook/addon-docs/blocks";

import Card from "../../src/components/ux/cube-ball.vue";
import { ref } from 'vue'

<Meta title="css/ball" component={Card} argTypes={{}} />

export const Template = (args, { argTypes }) => ({
  setup() {
    const list = ref([])
    return { args, list };
  },
  components: { Card },
  template: `<Card v-bind="args" v-model="list"  />`,
});

# ball

光从中心射出，阴影在底部

<Canvas>
  <Story
    name="default"
    args={{
      valueKey: 'id',
      labelKey: 'label',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="default" />
