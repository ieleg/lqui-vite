import { Meta, Canvas, Story, ArgsTable } from "@storybook/addon-docs/blocks";

import Card from "../../src/components/ux/counter.vue";
import { ref } from 'vue'

<Meta title="css/counter" component={Card} argTypes={{}} />

export const Template = (args, { argTypes }) => ({
  setup() {
    const list = ref([])
    return { args, list };
  },
  components: { Card },
  template: `<Card v-bind="args" v-model="list"  />`,
});

# masonry

监听高度和列数量的变化，记录垂直方向上顶部块的`bottom`和底部块的`top`值, 如果它们的差不为`gap`值，则表示顶部的空间没有被占满

<Canvas>
  <Story
    name="default"
    args={{
      valueKey: 'id',
      labelKey: 'label',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="default" />
