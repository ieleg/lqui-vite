import { Meta, Canvas, Story, ArgsTable } from "@storybook/addon-docs/blocks";

import RectV from "../../src/components/fabric2/mask.vue";

<Meta title="canvas/mask" component={RectV} argTypes={{}} />

export const Template = (args, { argTypes }) => ({
  setup() {
    return { args };
  },
  components: { RectV },
  template: `<rect-v v-bind="args" />`,
});

# cursor

## 控件
#### 通用  
<Canvas>
  <Story
    name="default"
    args={{
      valueKey: 'id',
      labelKey: 'label',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="default" />
