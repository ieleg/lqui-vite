import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs/blocks';

import Pie from '../../src/components/D3/pie.vue';

<Meta title="D3/pie" component={Pie} argTypes={{}} />

export const Template = (args, { argTypes }) => ({
  setup() {
    return { args };
  },
  components: { Pie },
  template: `<div style="height: 600px;width:600px"><pie v-bind="args" /></div>`
});

# PIE-chart
封装pie
## pie-chart
<Canvas>
  <Story
    name="default"
    args={{
      total: 50,
      tipText: (data) => '猪八戒的颜色' + data.color,
      data: Array.from({length: 10 }, (_, k) => ({
        value: 100+k+1,
        color: `#${(k+1)*10}${(10-k)*10}${(k+1)*10}`
      }))
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="default" />
