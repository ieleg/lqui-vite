import { Meta, Canvas, Story, ArgsTable } from "@storybook/addon-docs/blocks";

import SelectV from "../../src/components/ux/grid-card2.vue";
import { ref } from 'vue'

<Meta title="ux/grid-anmitaion" component={SelectV} argTypes={{}} />

export const Template = (args, { argTypes }) => ({
  setup() {
    const list = ref([])
    return { args, list };
  },
  components: { SelectV },
  template: `<select-v v-bind="args" v-model="list"  />`,
});


# grid-responsive2

<Canvas>
  <Story
    name="default"
    args={{
      valueKey: 'id',
      labelKey: 'label',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="default" />
