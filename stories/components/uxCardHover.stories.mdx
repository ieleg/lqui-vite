import { Meta, Canvas, Story, ArgsTable } from "@storybook/addon-docs/blocks";

import Card from "../../src/components/ux/card-hover.vue";
import { ref } from 'vue'

<Meta title="css/figure-card" component={Card} argTypes={{}} />

export const Template = (args, { argTypes }) => ({
  setup() {
    const list = ref([])
    return { args, list };
  },
  components: { Card },
  template: `<Card v-bind="args" />`,
});

# card

<Canvas>
  <Story
    name="default"
    args={{
      valueKey: 'id',
      labelKey: 'label',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="default" />
